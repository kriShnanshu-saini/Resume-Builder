import { ScaleLoader } from 'react-spinners';

const Loader = () => {
	return (
		<div className='w-screen h-screen flex items-center justify-center  '>
			<ScaleLoader color='#484FCD' />
		</div>
	);
};

export default Loader;
